import styled from "styled-components";

export const Label = styled.span`
  display: grid;
  place-content: center center;
  background-color: #fff;
  padding: 0.3rem;
  border-radius: 50%;
  cursor: pointer;
  /* svg {
    fill: ${(props) => (props.$isFavorite ? "#FFAF37" : "#D0D0D0")};
  } */
`;
